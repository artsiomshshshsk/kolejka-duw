# Environment configuration for DUW Queue Monitor
# Copy this file to .env and fill in your actual values

# Database configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=duw_queue
USE_PROXY=0

# Working hours configuration (24-hour format, Europe/Warsaw timezone)
WORK_START_HOUR=8
WORK_END_HOUR=18

# Proxy configuration - REQUIRED
# Replace with your actual proxy credentials
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password
PROXY_ADDRESS=your_proxy_address
PROXY_PORT=your_proxy_port

# Example:
# PROXY_USERNAME=user123
# PROXY_PASSWORD=pass456
# PROXY_ADDRESS=proxy.example.com
# PROXY_PORT=8080

# Telegram bot notifications (optional)
# If set, the app will notify when tickets_left goes from 0 to >0 for `odbior_karty`
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Note: You can set TELEGRAM_CHAT_ID to a channel username (e.g., @mychannel)
# Ensure the bot is added as an admin to the channel so it can post
